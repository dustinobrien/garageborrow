import { AppShell } from "../components/AppShell";

export default function Donate(): JSX.Element {
  return (
    <AppShell>
      <h1 className="font-heading text-3xl">Donate an item</h1>
      <p className="mt-3 opacity-80">Donation flow lands in a follow-up PR.</p>
    </AppShell>
  );
}
